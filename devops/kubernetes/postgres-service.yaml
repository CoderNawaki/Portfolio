apiVersion: v1
kind: Service
metadata:
  name: portfolio-postgres
spec:
  clusterIP: None
  selector:
    app: portfolio-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
